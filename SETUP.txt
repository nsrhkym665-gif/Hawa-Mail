Hawa Mail — ملحوظة مهمة

النسخة التي رفعتها على GitHub Pages ستعرض الواجهة فقط.
للبريد الحقيقي: Cloudflare Worker + KV + نطاق حقيقي + Email Routing.

الواجهة الجديدة فيها وضع عرض تلقائي إذا لم تجد /api/inbox، لذلك لن تظهر شاشة "جارٍ التحميل" الفارغة على GitHub Pages.

عند تشغيل Worker على نفس المشروع ستنتقل تلقائيًا للاستقبال الحقيقي.
